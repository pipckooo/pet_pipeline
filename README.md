# pet_pipeline


## Project Goal
To build a fully automated DevOps pipeline where:
1. Every code change triggers automated tests (CI).
2. Successful code is containerized in Docker.
3. The app is automatically deployed to a server (CD).